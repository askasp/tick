# feature: a plan (one pull request, or a stack of small ones), local's steps, then a pull request and its CI.
# Who solves each agent step (claude or opencode). MODEL_<step>= pins a model for it.
CLI_plan=opencode
CLI_implement=opencode
CLI_review=opencode
