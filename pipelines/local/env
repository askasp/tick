# local: code it, test it, review it; the branch is left for you to merge.
# Who solves each agent step (claude or opencode). MODEL_<step>= picks a model for it.
CLI_implement=claude
CLI_review=claude
