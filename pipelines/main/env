# main: local's steps, but in the repo's own checkout. Commits land on the branch it's on; nothing to merge.
# One task at a time per checkout, and the implementer waits while the checkout has uncommitted changes.
IN_PLACE=yes
CLI_implement=opencode
CLI_review=opencode
