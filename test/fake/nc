#!/usr/bin/env bash
# A stand-in for `nc -l` taking Google's login redirect: it reads the page it would serve, and prints the
# request a browser makes once you consent there (code 4/abc).
cat > /dev/null
printf 'GET /?code=4%%2Fabc&scope=x HTTP/1.1\r\nHost: 127.0.0.1\r\n'
