#!/usr/bin/env bash
# t path [TASK] — print the task's worktree:  cd "$(t path widget)"
. "$(dirname "$(readlink -f "$0")")/../lib/tick.sh"
task=$(task_dir "${1:-}") || exit 1
readlink -f "$task/work"
